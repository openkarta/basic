# Open Karta runtime data artifacts — large and regenerated by the pipeline.
data/*
!data/.gitkeep
.playwright-mcp/*

# Photon jar (96 MB binary) — re-download from the komoot/photon release.
photon/*.jar